const styles = (theme) => {
    return {
        app: {
            marginTop: 96,
            backgroundColor: theme.color.primary,
        },
        btn: {
            marginRight: 8,
        }
    }
}
export default styles;