const styles = (theme) => {
    return {
        app: {
            marginTop: 96,
            display: 'flex',
            justifyContent: 'space-between',
        },
    };
};
export default styles;
