const styles = (theme) => {
    return {
        app: {
            marginTop: 96,
            backgroundColor: theme.color.primary,
        },
        btn: {
            margin: '0 12px',
        }
    }
}
export default styles;