const styles = (theme) => {
    return {
        App: {
            width: '100vw',
            height: '100vh',
        },
        backgroundColor: {
            backgroundColor: theme.color.primary,
        },
        backgroundDark: {
            backgroundColor: theme.color.primaryDark,
        },
    };
};
export default styles;
