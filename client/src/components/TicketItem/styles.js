const styles = (theme) => {
    return {
        root: {
            transition: 'all 0.2s ease',
            '&:hover': {
                marginTop: -12,
            },
            borderRadius: 10,
        },
    };
};
export default styles;
