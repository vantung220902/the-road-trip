const styles = (theme) => {
    return {
        root: {
            flexGrow: 1,
            maxWidth: 500,
        },
        icon: {
            backgroundColor: 'none',
            fontSize: 18,
            color: '#000',
        },
    };
};
export default styles;
